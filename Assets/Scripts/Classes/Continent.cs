using UnityEngine;
using System.Collections;

public class Continent : MonoBehaviour {

   // Indicateur pop, prodNourriture, besoinNourriture, qualiteAir, qualiteTerre, qualiteMer, biodiversite;

    
	// Use this for initialization
	void Start () {
        //pop = new Indicateur(50000);
        //prodNourriture = new Indicateur(50000);
        //besoinNourriture = new Indicateur(500000);
        //qualiteAir = new IndicateurAvecConstante(100, 100, 5);
        //qualiteMer = new IndicateurAvecConstante(100, 100, 5);
        //qualiteTerre = new IndicateurAvecConstante(100, 100, 5);
        //biodiversite = new Indicateur(10000000);
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
